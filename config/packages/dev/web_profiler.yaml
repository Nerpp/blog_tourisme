web_profiler:
    toolbar: true
    intercept_redirects: false

framework:
    profiler:
        only_exceptions: false
        collect: true
        # The WebCron route fixes this attribute to false so HTTP Basic
        # credentials are never serialized in a development profile.
        collect_parameter: _collect_profile
